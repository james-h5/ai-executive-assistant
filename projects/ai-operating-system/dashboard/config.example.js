// Copy this file to config.js and fill in your keys.
// config.js is gitignored — never commit your real keys.

const CONFIG = {
  clickup: {
    apiKey: 'YOUR_CLICKUP_API_KEY'   // found in ClickUp → Settings → Apps
  }
};
