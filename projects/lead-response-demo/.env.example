# Anthropic — get from console.anthropic.com/api-keys
ANTHROPIC_API_KEY=

# Trigger.dev — get from cloud.trigger.dev/projects > your project > API keys
TRIGGER_SECRET_KEY=

# Deliberately no TWILIO_*, CLICKUP_*, or FIRECRAWL_* keys here.
# This demo never sends a real SMS/email — see README.md for why.
# Do not add Twilio credentials to this project.
