import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  // Use your existing trigger.dev project ID from cloud.trigger.dev
  // Or create a new project and paste the ID here
  project: "proj_omtdpuzktsfskxspmnos",
  dirs: ["./src/trigger"],
  maxDuration: 300,
});
